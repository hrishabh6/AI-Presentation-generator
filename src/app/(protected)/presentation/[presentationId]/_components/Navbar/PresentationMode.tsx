import React from 'react'

type Props = {
    onClose : () => void
}

const PresentationMode = ({
    onClose
}: Props) => {
  return (
    <div>PresentationMode</div>
  )
}

export default PresentationMode